---
description: Mark this Claude session as pending (waiting on PR, async action, etc.) in the claude-track TUI
---

Run this bash command to mark the session as pending:

```bash
claude-track set-pending "$ARGUMENTS"
```

Report whether it succeeded or failed. Do nothing else.
